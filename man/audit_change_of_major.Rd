% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/change_of_major_audit.R
\name{audit_change_of_major}
\alias{audit_change_of_major}
\title{Audit change-of-major submissions against student records}
\usage{
audit_change_of_major(
  change_of_major,
  student_records,
  program_names = UWLdbr::get_program_name_db(),
  pre_professional_track_alias_lookup =
    UWLdbr::get_lookup_by_name("pre_professional_track_alias"),
  acceptable_program_alternatives = tibble::tribble(~requested_program,
    ~acceptable_program),
  exception_output = NULL,
  detail_output = NULL
)
}
\arguments{
\item{change_of_major}{A data frame containing change-of-major submissions,
or a path to a CSV file. The data must include the form columns used by
the audit, including \verb{Reference Number}, \verb{Date Submitted}, \verb{Student ID},
student name and comment fields, the desired-program fields, and the
minor-or-other-program indicator.}

\item{student_records}{A data frame containing student records, or a path to
an Excel workbook. Excel input is read with its first row skipped because
the report field names are expected on row 2. Required columns are \code{ID},
\verb{Campus ID}, \verb{1st Major}, and \verb{Plan 2} through \verb{Plan 4}.}

\item{program_names}{A program-name mapping data frame, a CSV path, or the
named list returned by \code{\link[UWLdbr:get_program_name_db]{UWLdbr::get_program_name_db()}}. Defaults to the
current UWL program-name database. The mapping must contain \code{acad.plan},
\code{descr}, \code{transcript.description}, \code{career}, \code{type}, \code{first.term}, and
\code{last.term}.}

\item{pre_professional_track_alias_lookup}{A data frame with columns \code{alias}
and \code{program}. Each program must resolve to an undergraduate \verb{Pre Prof}
plan. Defaults to the \code{pre_professional_track_alias} lookup from UWLdbr.}

\item{acceptable_program_alternatives}{A data frame with columns
\code{requested_program} and \code{acceptable_program}. Use it to treat an alternate
undergraduate academic-plan code as satisfying a requested program.}

\item{exception_output}{Optional path prefix for a dated CSV containing only
exception rows. Do not include the date or \code{.csv}; both are appended.}

\item{detail_output}{Optional path prefix for a dated CSV containing all
comparison rows. Do not include the date or \code{.csv}; both are appended.}
}
\value{
A named list with four tibbles: \code{summary} (audit counts),
\code{exceptions} (nonmatching or unresolved comparisons), \code{detail} (all
comparisons), and \code{matched_submissions} (submissions joined to student
records).
}
\description{
Compares requested majors, second majors, tracks, concentrations, and minors
from change-of-major submissions with the programs currently recorded for
each student. Student records are matched first by Campus ID and then by
internal ID. Program descriptions are translated to academic-plan codes
using the UWL program-name database.
}
\details{
Concentrations are treated as embedded in the primary-major code.
Requested second majors may match either the second-major plan code or its
primary-major equivalent. When output prefixes are supplied, files are
written as \verb{<prefix>_YYYY-MM-DD.csv}.
}
\examples{
\dontrun{
alternatives <- tibble::tribble(
  ~requested_program, ~acceptable_program,
  "RSDMSEV.BS", "PREDMSE.BS",
  "NMT.BS", "PRENMT.BS"
)

result <- audit_change_of_major(
  change_of_major = "change_requests.csv",
  student_records = "student_records.xlsx",
  acceptable_program_alternatives = alternatives,
  exception_output = "output/change_of_major_exceptions",
  detail_output = "output/change_of_major_audit_detail"
)

result$summary
result$exceptions
}

}
